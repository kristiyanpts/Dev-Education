<section class="details-page">
  {{#with book}}
    <h1>Details</h1>
    <article class="details-card">

      <article class="details-card-text">
        <h2>Title:<br /> {{title}}</h2>
        <h3>Author: {{author}}</h3>
        <h3>Stars: {{stars}}</h3>
        <h3>Genre: {{genre}}</h3>
        <h3>Review: {{review}}</h3>

        {{#if isUser}}
          <div class="buttons">
            {{#if isOwner}}
              <a href="/books/{{_id}}/edit" class="btn-edit">Edit</a>
              <a href="/books/{{_id}}/delete" class="btn-delete">Delete</a>
            {{else}}
              {{#if canWishlist}}
                <a href="/books/{{_id}}/wishlist" class="btn-wish">Wish to read</a>
              {{else}}
                <p class="wish-pub">You already added the book to your wish list</p>
              {{/if}}
            {{/if}}
          </div>
        {{/if}}

      </article>

      <article class="details-card-image">
        <img src="{{image}}" />
      </article>

    </article>
  {{/with}}
</section>