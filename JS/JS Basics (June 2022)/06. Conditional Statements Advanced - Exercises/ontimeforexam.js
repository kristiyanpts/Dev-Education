function Main(Input) {
    //TO DO LATER WHEN I WANT TO THINK

    let startEh = Number(Input[0]);
    let startEm = Number(Input[1]);
    let arriveEh = Number(Input[2]);
    let endEm = Number(Input[3]);

    if (startEh > arriveEh)
    {

    }
}